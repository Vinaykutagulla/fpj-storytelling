###############################################
# Environment Variable Template (.env.example) #
###############################################
# Copy this file to .env.local for local development.
# NEVER commit your real .env.local with secrets.

# --- Public Site URL (used for canonical + sitemap) ---
NEXT_PUBLIC_SITE_URL=https://firstpharmajob.com

# --- Supabase (Optional: enables persistence) ---
# If omitted, application submissions fall back to in-memory array.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# (Anon key not required yet because we have no client-side Supabase usage)

# --- Admin Security ---
# Random long string used as Bearer token for /api/partner-applications/admin
ADMIN_BEARER_TOKEN=

# --- Operational / Telemetry ---
NEXT_TELEMETRY_DISABLED=1

# --- Healthcheck Overrides (Optional) ---
HEALTHCHECK_BASE_URL=
HEALTHCHECK_TIMEOUT=7000
HEALTHCHECK_POST=false

# Notes:
# 1. Set these in Vercel Project Settings for Production/Preview.
# 2. Ensure Supabase table 'partner_applications' exists with required columns.
# 3. Leave unset to disable a feature (e.g., omit SUPABASE_* to skip persistence).
